# P6 — HTTP API, gateway and authorization

> Read `ARCHITECTURE.md` at the repo root first — it is the authoritative design record. Follow
> `.claude/PACKET_RULES.md`. You own only the packages listed below; if you need to change anything
> else, stop and report rather than editing it.

**You own:** every `*/web` package, `planning/application`, `planning/persistence`, `api-gateway/**`,
Flyway migrations **V30–V39**, and the *authorization* and *plan history* sections of
`spec/ProfileAndFundsSpecTest.java`.
**Wave:** 3, after P1, P2, P3 and P4 have merged.

**Keys you need:** already provided. The Supabase project exists and `SUPABASE_URL` and
`SUPABASE_JWKS_URI` are in the `.env` in your worktree. **Do not create a project** — P8 is signing
users into this one in parallel with you, and a second project would mean the gateway validates
tokens that the client never issues.

**You also own putting the application into Docker Compose**, because no other packet does and
nothing runs end to end without it. Today `docker-compose.yml` holds only Postgres and Redis. Add:

- a `Dockerfile` for `budget-core` and one for the gateway;
- both as Compose services, where **only the gateway publishes a port** — `budget-core` gets none,
  which is exactly what your isolation test asserts. Without `budget-core` actually being in Compose,
  that test verifies nothing;
- the gateway on a host port **other than 8080**, which the jobs-tracker stack on this machine
  already holds. Record the choice in the compose file's port comment beside the others.

**Done** includes `wsl docker compose up` bringing up the whole backend with no manual step, and the
gateway answering a health check. That is the moment this stops being a set of tested modules and
becomes an application.

**P8 is running at the same time as you**, building against the endpoints you write. It has been told
not to guess your request and response shapes, and to stop at the onboarding flow until your work
reaches `main`. So your committed `.http` file is not only documentation — it is the contract P8
builds the dashboard against. Commit it early, as soon as the endpoint shapes are settled, rather than
at the end.

---

## Build

The geo endpoints, profile, overrides, goals, `POST /plan`, `GET /plan`, `/plan/history`. RFC 9457
problem details, which are native in Boot 4.

**The catalogue endpoints already exist — extend them, do not rewrite them.** P2's brief required it
to build the cascading country/city/manual-form endpoints, so `costofliving/web/CatalogueController`
landed before this packet despite `*/web` being listed above as yours. That overlap is a mistake in
how these two briefs were written, not a rule P2 broke. The endpoints are covered by
`CatalogueEndpointIT`, including an assertion that no constant name reaches a payload. Adding
problem details and the remaining routes around them is the work; deleting and re-deriving them
would lose that coverage for nothing.

`PlanAssembler` in `planning/application` is the **sole translator** between cost-of-living and
planning. It takes `ResolvedBaseline` objects and flattens them to bare `Money` before the math sees
them. **Confidence travels to the view, never into the formula** — the moment the math can see it,
someone writes `if (confidence == ESTIMATED)` inside the calculation and the branch sprawl begins.

Plans are **append-only snapshots**, so adding a goal never rewrites what the plan said before. That
is what lets `/plan/history` show exactly what changed when a goal was added.

## Earmarking the considered balance — added after P4

> P4 built `ConsideredFunds` but could not build this half: it lives in `planning/application`,
> which you own. P4 shipped the other channel, the runway figure, and stopped at the boundary.
> Without this section a user with $50,000 in the bank is told a $20,000 car is infeasible.

There are **two quantities**, and conflating them is the trap. `monthlyIncome` is a rate and
`consideredBalance` is a stock — `ConsideredFunds` says so in its own javadoc. The surplus is what
funds goals each month; the balance is a pile that already exists and that no part of the surplus
formula touches.

`PlanAssembler` is where the stock reaches the plan, and it does so by **raising a goal's `saved`**:

```
remaining()           = target − saved
requiredMonthly(asOf) = remaining().spreadOver(monthsRemaining(asOf))
```

Crediting $20,000 to a $20,000 car goal makes `requiredMonthly` zero and frees the whole monthly
surplus for the next goal. No new concept — an existing lever, moved before the allocator runs.

**Three decisions, already made. Do not re-open them.**

1. **Order.** Earmark in descending priority. The user may additionally nominate **one goal to
   finish first**, and that goal takes the balance ahead of priority order. This affects earmarking
   only — the allocator keeps ranking priority-then-cheapest and keeps receiving one `Money`.

2. **Reversible at any time.** The user can pull money back out of scope whenever they like, by the
   same means they set it: exclude an account, flag a deposit, lower the share, or
   `ManualConsideredFunds.revisedTo(...)`. Re-running the plan afterwards must hand the money back
   — a user who cannot withdraw from a plan stops putting money in. Snapshots are append-only, so
   this is a new snapshot, never an edit to an old one.

3. **`saved` is derived from the balance, never typed independently.** This is the one that bites.
   If a goal could carry a hand-entered "already saved: $20,000" *and* receive a share of the
   considered balance containing those same dollars, the goal reads as funded twice. It is the third
   instance of a bug class this codebase already designs against — counting a savings transfer as
   spending, and taxing already-net income — except this one errs **optimistic**, which is worse:
   nothing looks wrong, the plan is simply a lie. Deriving `saved` from one source makes it
   impossible by construction rather than by a rule someone has to remember. **Write the test.**

**The hard constraint is unchanged.** `ConsideredFunds` must never be passed to the allocator.
`AllocationRequest` takes a single `Money` surplus, which is what makes `GreedyPriorityAllocator`'s
optimality argument hold — a stock divisible across goals is a second resource, and greedy is not
optimal for two. `ArchitectureTest.allocationEngineIsFrameworkFree` already forbids
`planning.domain` from depending on `..profile..`; if you find yourself wanting to relax it, the
design has gone wrong, not the rule.

Whatever is left unearmarked still drives `Runway.of(funds, monthlyDeficit)` for display, so the two
channels never disagree about the same dollars.

## The gateway

Copy the gateway files out of `jobs-tracker-distributed-system`. **That repository is read-only —
never edit or commit to it**, even for something that looks improvable in passing. Make every
adaptation in the copies.

Keep its rate limiting unchanged. Extend auth with Supabase JWT validation against Supabase's JWKS,
and remove whatever the previous auth mechanism was where Supabase supersedes it, rather than leaving
it dead alongside. The gateway injects the user id; every `budget-core` query is scoped by it.

Note that the jobs-tracker filter is a **reactive** `WebFilter` (`ServerWebExchange`, `Mono`), which
suits the gateway and does not port into `budget-core` — that module is on the servlet stack. Copy it
into `api-gateway/`, not into the core.

### The security chain already in `budget-core`

`com.hasan.budget.security.InternalServiceSecurityConfiguration` exists and **permits every request**.
It is not an oversight and it is not a stub to delete carelessly. The resource-server starter is on
the classpath, so with no configuration at all Spring Boot locks every route behind HTTP Basic and a
generated password — every endpoint in the service answers 401, including the health check Compose
depends on, and the symptom reads as a bug in whichever controller is being tested. P2 added the
chain so the service is runnable at all.

Its reasoning is written out in that file and comes down to this: re-validating the caller's token
here would check the same credential twice in the one place a second answer could only disagree with
the first. The real controls are network isolation and per-user query scoping, which are both yours.

**Your decision, not a leftover.** If you want defence in depth — validating the Supabase JWT here as
well, or requiring the injected user-id header so a misconfigured gateway fails loudly instead of
quietly serving anonymous traffic — that bean is where it goes and nothing else moves.
`CatalogueEndpointIT#theGatewayIsNotAskedToLogIn` guards the regression; update it rather than
deleting it if you tighten the policy.

## The two tests worth more than the rest of the packet

1. **User A cannot read user B's plan.** Bank data is the most sensitive thing here, and one user
   reading another's is the failure that ends the product.
2. **`budget-core` is unreachable from outside the Compose network.** This is what justifies keeping
   the gateway at all: `budget-core` trusts the injected user-id header, so without network isolation
   that header is forgeable and the first test becomes unenforceable in production.
   `docker-compose.yml` publishes only the gateway's port, and the comment in that file is
   load-bearing.

## You own the composition, and it currently has a bug

**Added after P1 merged, at Hasan's instruction, because this is where the fix belongs and he did not
want it forgotten.** Nothing below is optional polish.

`SurplusCalculation` and `GreedyPriorityAllocator` are each correct on their own. **Joining them
naively produces a plan that lies.** Measured on the merged code, with the scenario in
`SurplusSpecTest`:

| | |
|---|---|
| Reported surplus | $2,070 |
| Cash actually left after real spending | $1,500 |
| Goals need | $2,250 |
| Cuts the plan suggests | **$180** |
| What the user must really change | **$750** |

The surplus is **not** money in hand. It is what the user would have *if* they cut to baseline and to
the floor — $570 here. Hand the allocator the surplus plus each category's **observed** spend as cut
candidates and it proposes $180, `residualGap` reads $0.00, and the user reasonably concludes the goal
is reachable. They follow the advice exactly and land $570 short.

Two things are required of you, and they are separate:

1. **Show both numbers wherever a plan reaches a person.** `SurplusBreakdown.assumedReduction()`
   already carries the $570, and `totalReductionNeeded(suggestedCuts)` already sums it with the
   allocator's cuts. Neither is optional in the response body. A plan that reports `tradeoffs`
   without `assumedReduction` beside it is the bug, not a simplification of it.

2. **Hand the allocator headroom, not observed spend.** A cut candidate must be what is *still*
   available to cut after the reduction the surplus already assumed — otherwise the allocator offers
   up money the formula has spent twice. For a `CAP_AT_BASELINE` category the surplus already charged
   only `min(actual, baseline)`, so there is no headroom left in it unless you intend to propose
   cutting below the local baseline, which is a different and louder thing to say. For
   `DISCRETIONARY` categories the headroom is what observed spending exceeds the floor, and it is a
   single pool across all of them, because the floor is one figure and not a per-category budget.

**Use each line's own `rigidity()`, never `category.defaultRigidity()`.** `CategoryLine` carries it
precisely so a gym the user marked `LOCKED` reaches the allocator locked. `CategoryLine.isCuttable()`
applies the same two conditions the allocator does; prefer it to rolling your own filter.

Write the composition test that fails today: goals short by some amount, follow only the suggested
cuts, and assert the user's real cash position reaches what the goals need. That test is worth more
than either of the two below.

## Done when

The 6 authorization and plan-history behaviours are green, plus a committed `.http` file walking the
full happy path.

Earmarking postdates the specs, so write its tests too, to the same standard. At minimum: a goal
covered by the balance needs nothing monthly; a nominated goal takes the balance ahead of a
higher-priority one; taking money back out of scope returns it to the plan on the next run; and the
same dollars can never reach one goal twice.

## Reaching the second product surface — added after P7

> P7 built F8 and F9 in `planning/domain/decision`, framework-free, with all 19
> `SpendDecisionSpecTest` behaviours green. It could not expose them: every `*/web` package and
> `planning/application` are yours. Nothing in the build list above mentions them, so as things stand
> the affordability check and rebalancing would ship unreachable — built, tested, and impossible to
> use.

Two endpoints, and they are thin. The arithmetic is done; what is missing is the assembly, which is
exactly the translator role you already hold.

**`SpendDecision.decide(SpendDecisionRequest)`** answers "can I afford this today" over any category.
The request wants five things, four of which already exist somewhere:

| Field | Where it comes from |
|---|---|
| `category` | the user's request |
| `allowance` | the resolved baseline for that category — the user's own figure where they set one, `BaselineResolver` otherwise, or the discretionary floor for a `DISCRETIONARY` category |
| `spentMonthToDate` | sum of `SPEND` transactions in that category this month. `TRANSFER_INTERNAL` is not spending; the double-count trap applies here exactly as it does in the surplus |
| `purchase` and `ladder` | `PriceLadder.fromDiningBaseline(baseline, observedTickets)` — see the gap below |
| `asOf` | passed in, never read. The domain may not touch a clock and ArchUnit enforces it |

**`Rebalancing.apply(RebalanceRequest)`** moves money between lines. Each `BudgetLine` needs its
`floor`, which is that line's localised baseline, resolved before it arrives — the same flattening
`PlanAssembler` already does for the surplus. Use the line's own `rigidity()`, never the category
default, for the reason stated above.

**`Share` is how dollars and percentages stay one number.** The user may type $700 or 10%;
`Share.ofPercent(percent, pool)` converts against the considered pool, and the request carries only
the resulting `Money`. Do not store a percentage: the earmarking section above already explains why
`consideredBalance` is the stock this is a share of.

**`ResolutionOption.COUNT_MORE_OF_YOUR_BALANCE` is not a new mechanism.** It is the earmarking lever
you are already building, surfaced as one of the two honest answers when an increase cannot be
absorbed. Wire it to that, not to something new.

### The one piece no brief owns

The observed merchant averages. P3's brief has it persist `merchant_entity_id` from the first import
specifically so this needs no backfill, and P7 consumes a `List<ObservedTicket>` — average ticket and
sample size per merchant. **Nobody is told to write the aggregation between those two facts.**

It cannot live in `planning.domain`, which may not import `..ingestion..`. P3's brief has now been
asked to expose the aggregate behind a port in `ingestion/port`; mapping that port's result to
`ObservedTicket` belongs to you, as the sole translator. If P3 merged without it, say so rather than
reaching into `ingestion/persistence` — and note that the feature degrades honestly without it: with
no observations every band is `ESTIMATED` and says so, which is the cold-start case P7 already tests.

**Never render a constant name.** `SpendBand`, `SpendVerdict`, `PriceBasis`, `RebalanceOutcome` and
`ResolutionOption` each carry `label()` plus `covers()` or `meaning()`, written for a person, for the
same reason `CatalogueEndpointIT` asserts no constant reaches a payload. Extend that assertion to
these routes.
