# Several guarded packages (costofliving, profile, ingestion, planning.domain.surplus) are designed
# but not yet created. Without this, ArchUnit fails a rule whose subject set is empty, which would
# mean the guardrails could not be committed until the code they guard exists - exactly backwards.
archRule.failOnEmptyShould=false
